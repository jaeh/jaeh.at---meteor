"use strict";
/*
var MessagesController = new Stellar.Controller("messages");

MessagesController.index = function() {
  if(user = checkAuth(args.auth)) {
    var page = {
      title: 'messages',
      body: '',
      footer: ''
    };
    Meteor.subscribe('allmessages',function() {
      
      var messages = Messages.find();
    
      Stellar.render('messages_page', {page: page, messages: messages});
    });
  }
  Stellar.redirect('login');
}

MessagesController.show = function(args) {
  Stellar.redirect('/messages');
}

*/
